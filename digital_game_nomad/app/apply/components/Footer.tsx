// slice
import styles from '../styles/Apply.module.scss';

export default function Footer() {
  return (
    <div className={styles.footerContainer}>
      <p>신청서 제출 후 검토를 거쳐 승인 여부를 안내드립니다.</p>
    </div>
  );
}
